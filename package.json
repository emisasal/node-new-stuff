{
  "engines": {
    "node": ">=20.6.0"
  },
  "name": "new-stuff",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "dev": "node --env-file=.env --env-file=.env.development --watch --watch-preserve-output -r ts-node/register src/server.ts",
    "build": "tsc",
    "start": "node --env-file=.env --env-file=.env.production dist/server.js",
    "test": "node node_modules/.bin/jest"
  },
  "keywords": [],
  "author": "Emiliano Sasal",
  "license": "MIT",
  "description": "A simple API to test new stuff in Node.js",
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/jest": "^29.5.13",
    "@types/node": "^22.5.0",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.4"
  }
}
